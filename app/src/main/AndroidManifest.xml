<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rongqi.hua.rongqihua">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 权限声明 -->
    <!-- 访问网络状态-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 外置存储存取权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".application.RongqiApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.RegistActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RegisInfotActivity" />
        <activity android:name=".activity.MyDevelopersActivity" />
        <activity android:name=".activity.MyChidrensAvtivity" />
        <activity android:name=".activity.AddDeveloperActivity" />
        <activity android:name=".activity.MyRegisInfoDetailActivity" />
        <activity android:name=".activity.AddChildActivity" />
        <activity android:name=".activity.AboatActivity" />
        <activity android:name=".activity.ModifyRegistInfoActivity" />
        <activity android:name=".activity.ZhengceActivity" />
    </application>

</manifest>